# PUDDY-sp-frontend
- 기본적으로 yarn 설치
  - M1 맥일경우 brew install yarn --without-node // node X 일시
  - node O -> brew install yarn
  - npm 있을 시 -> npm i -g yarn
- 그 다음 command에 yarn 쳐서 기본 라이브러리 다 설치
- 이후 생긴 폴더로 들어가서 yarn dev로 실행